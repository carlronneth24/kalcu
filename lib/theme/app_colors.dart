import 'package:flutter/material.dart';

/// Design tokens pulled from the Figma file.
class AppColors {
  static const background = Colors.black;
  static const buttonBg = Color(0xFF242424);
  static const accent = Color(0xFFEC8116); // orange operator color
  static const textLight = Color(0xFFECECEB);
  static const textWhite = Colors.white;
}
